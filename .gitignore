__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.local
.claude/settings.local.json
.claude/worktrees/
.agent/tasks/*.local.md

# Test capture images (not source — generated during sessions)
captures/

# Calibration marker (per-machine)
.calibrated

# Runtime / per-session data — regenerated by tools, not source-of-truth
server.log
data/observe_frames/
data/spatial_memory.json
data/spatial_memory.json.tmp
data/observation_log.md
data/current_calibration.json
data/diag_report.json
data/test_shots/
data/perceive_log/
data/client_logs/

# Throwaway debug frame captures dropped in the repo root during sessions
/frame_*.jpg

# Harness self-improvement runtime state (friction log + review marker; per-machine)
.claude/harness/

# SO-101: generated assets (reproducible via build_glb.py / MuJoCo). Source STL
# + MJCF under sim/ ARE vendored; these are derived/preview artifacts.
src/robots/so101/meshes_glb/
src/robots/so101/sim/*.png
src/robots/so101/sim/*.gif
